# Copy to .env.local and fill in your values

# ── UPI Payment ────────────────────────────────────────────────────────────
# Your registered UPI ID (e.g. yourname@upi, yourname@okaxis)
NEXT_PUBLIC_UPI_ID=merchant@upi

# Price in INR (shown in QR code and order summary)
NEXT_PUBLIC_PRICE=49

# ── Razorpay ───────────────────────────────────────────────────────────────
# Get these from: https://dashboard.razorpay.com → Settings → API Keys
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_key_secret_here

# Get this from: Dashboard → Settings → Webhooks → Add Webhook → "Secret" field
# Your webhook URL to paste there: https://yourdomain.com/api/webhook
PAYMENT_WEBHOOK_SECRET=your_webhook_secret_here

# ── Draft storage ──────────────────────────────────────────────────────────
# Drafts are stored in ./.drafts by default.
# Override with an absolute path if needed (e.g. on a mounted volume).
DRAFT_DIR=.drafts

# Draft TTL in milliseconds (default: 86400000 = 24 hours)
DRAFT_TTL_MS=86400000
